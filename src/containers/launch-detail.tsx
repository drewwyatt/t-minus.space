import * as React from 'react';

import { Link } from "react-router-dom";

const LaunchDetail = () => (
    <div>
        <h1>Launch Detail</h1>
        <Link to="/">Home</Link>
    </div>
);

export default LaunchDetail;